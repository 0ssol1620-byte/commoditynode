name: Daily Commodity Price Update

on:
  schedule:
    - cron: '0 0 * * *'  # 매일 KST 09:00 (UTC 00:00)
  workflow_dispatch:      # 수동 트리거도 가능

jobs:
  update-prices:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Update commodity prices and chart data
        run: python scripts/update_prices.py

      - name: Commit and push if changed
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "GitHub Actions"
          git add assets/data/prices.json _data/prices.json assets/data/chart-data.json
          git diff --staged --quiet || git commit -m "📊 Auto-update: commodity prices $(date +'%Y-%m-%d')"
          git push
